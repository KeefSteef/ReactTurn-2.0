import {ADD_TO_STATICS, REDRAW_STATE_ELEMENT, UPDATE_AFTER_REMOVE, PUSH_OBJ_AFTER_REMOVE} from "../actionsType/actionsType";

const initialState = {

}


export default function rootCardOptions(state = initialState, action){
    switch (action.type){


        default : return state
    }
}







